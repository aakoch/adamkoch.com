import { PrefetchDirective } from './prefetch.directive';

describe('PrefetchDirective', () => {
  it('should create an instance', () => {
    const directive = new PrefetchDirective();
    expect(directive).toBeTruthy();
  });
});
