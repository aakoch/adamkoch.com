.row
  .col.g-2
    .card
      .card-body
        h5.card-title {{ postTitle }}
        .card-text 
          .date Posted {{ date | date:"YYYY-MM-dd" }}
          div(v-bind:id="id")
          p {{ excerpt }}
          .linkies
            a.href(routerLink="{{link}}") Read
            a.href(routerLink="{{link}}")  more
            | &nbsp; 
            a.href(routerLink="{{link}}")
              i(class='fas fa-angle-double-right')